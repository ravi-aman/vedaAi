# Design Reference

Reference screenshots supplied in assignment must be placed here for visual sign-off:

- `upload-desktop.png` — Upload screen desktop (centered heading, two large dashed cards, teacher illustration, orange CTA)
- `upload-mobile.png` — Upload screen mobile (compact header, stacked cards)
- `processing.png` — Processing state (centered spark/AI visual, “Extracting…”)
- `result-desktop.png` — Results split-pane (question list | viewer with highlights)
- `result-mobile.png` — Results mobile (single column navigation)

**Status:** Screenshots not included in repository (no image files found in workspace at implementation time). Repository owner should add them here before visual sign-off. UI was implemented to match described visual system: spacing rhythm, typography hierarchy, card radius, orange accent #FF6B2C, sidebar 200px, header 56px, etc.

No pixel-perfect verification completed without source images — documented honestly per spec §99.
